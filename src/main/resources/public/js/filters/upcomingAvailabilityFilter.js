
    var assignforce = angular.module( "batchApp" );

    assignforce.filter( "upcomingAvailability", function() {
        return function(items) {

            var now = new Date().getTime();
            var result 
        };
    });